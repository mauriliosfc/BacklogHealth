// Gerado pelo scripts/release.js — não editar manualmente
// Token injetado em build time via FEEDBACK_TOKEN; vazio em desenvolvimento
module.exports = { token: '', repo: 'mauriliosfc/BacklogHealth' };
